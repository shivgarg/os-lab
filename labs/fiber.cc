#include "labs/fiber.h"

void g(addr_t* pmain_stack, addr_t* pf_stack, int* pret, bool* pdone,int arg){


}

void shell_step_fiber(shellstate_t& shellstate, addr_t& main_stack, addr_t& f_stack, addr_t f_array, uint32_t f_arraysize){

 		// if(shellstate.init_fiber)
 		// {
 		// 	shellstate.fiber_done=false;;
 		// 	shellstate.fiber_out[0]='\0';
 		// 	stack_init4(f_stack,&f_array,f_arraysize,&g,&main_stack,&f_stack,shellstate.fiber_out,shellstate.done,shellstate.fiber_arg);
 		// }
 		// else if(!shellstate.fiber_done)
 		// {
			// stack_s 			
 		// }

}

